{
  "name": "sametab-server",
  "version": "1.0.0",
  "description": "WebSocket signaling server for SameTab Chrome extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ws": "^8.17.1"
  }
}
